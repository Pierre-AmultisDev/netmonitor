# NetMonitor Test Environment Configuration
# Deze file wordt gebruikt door pytest voor test configuratie

# SOC Server Configuration
SOC_SERVER_URL=https://test-soc.netmonitor.local:8080

# Sensor Configuration
SENSOR_ID=test-sensor-001
SENSOR_LOCATION=Test Location
SENSOR_TOKEN=test-token-12345

# Database Configuration (voor integration tests)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=netmonitor_test
DB_USER=test_user
DB_PASSWORD=test_password

# Logging
LOG_LEVEL=DEBUG
